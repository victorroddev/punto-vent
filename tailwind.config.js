module.exports = {
    content: [
        "./templates/**/*.html", // Busca archivos .html en la carpeta templates
        "./static/**/*.js" // Busca archivos .js en la carpeta static
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}